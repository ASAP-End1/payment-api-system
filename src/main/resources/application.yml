spring:
  application:
    name: payment-demo

  profiles:
    active: local    # application-local.yml 파일 생성해 local에서 DB연결

  datasource:
    # localhost:3306 뒤의 'payment_db'는 실제 생성하신 스키마(데이터베이스) 이름으로 바꿔주세요.
    url: jdbc:mysql://localhost:3306/test
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root        # MySQL 아이디 (보통 root)
    password: 1234 # MySQL 설치 시 설정한 비밀번호로 변경하세요!

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true      # (선택사항 - SQL 로그 확인)
    properties:
      hibernate:
        format_sql: true  # (선택사항 - SQL 포맷팅)

    defer-datasource-initialization: true

  sql:
    init:
      mode: always

  # 클라이언트 API 계약 설정 파일 import
  config:
    import: optional:classpath:client-api-config.yml

  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML
    encoding: UTF-8

  security:
    user:
      name: demo
      password: demo123

server:
  port: 8080
  servlet:
    context-path: /

# PortOne Configuration
portone:
  api:
    base-url: https://api.portone.io
    secret: ${PORTONE_API_SECRET:83zJhqvP6vUebfsozbuBTUeMpgepZfhPR5IssODyNwYI0pqofdIDpt2GwgNNLAobP56Wf1YeOW6xiQSY}
  store:
    id: ${PORTONE_STORE_ID:store-f6bd9fab-9ae2-4dfd-a7e5-5d7b5b800298}
  channel:
    kg-inicis: ${PORTONE_CHANNEL_KG:channel-key-3d7869fd-306a-4597-8f7f-d1c72cc2608c}
    toss: ${PORTONE_CHANNEL_TOSS:your-toss-channel-key}
  webhook:
    secret: ${PORTONE_WEBHOOK_SECRET:whsec_IEyjsd++0+1b/+QrYeDaa+gAfU3htpucGJAqa4hHoT0=}
# JWT Configuration
jwt:
  secret: ${JWT_SECRET:UjGDxgrzCb9XBOmnXPF6eruETQamJKYSacLEXUz7VV8=}
  token-validity-in-seconds: ${JWT_VALIDITY:86400}  # 24 hours

# UI Branding Configuration
app:
  ui:
    branding:
      app-name: CommerceHub
      tagline: 통합 커머스 플랫폼
      logo-text: 🏪 CommerceHub

logging:
  level:
    com.bootcamp.paymentdemo: DEBUG
    org.springframework.security: INFO